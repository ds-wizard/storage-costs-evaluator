name:                storage-costs-evaluator
version:             0.1.0.0
github:              "DataStewardshipWizard/storage-costs-evaluator"
license:             MIT
author:              "Marek Suchánek"
maintainer:          "marek.suchanek@dsw.fairdata.solutions"
copyright:           "2018 Marek Suchánek"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/githubuser/storage-costs-evaluator#readme>

dependencies:
- base >= 4.7 && < 5
- data-default
- lens

library:
  source-dirs: src
  default-extensions:
  - TemplateHaskell
  - OverloadedStrings
  - MultiParamTypeClasses
  - FunctionalDependencies
  - TypeSynonymInstances
  - FlexibleInstances

executables:
  storage-costs-evaluator-exe:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - storage-costs-evaluator

tests:
  storage-costs-evaluator-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - storage-costs-evaluator
